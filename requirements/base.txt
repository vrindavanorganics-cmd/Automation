# Cross-platform core dependencies (installable and testable in this browser workspace)
#
# Native/compiled packages use a minimum-version floor (>=) rather than an
# exact pin, so pip picks a release with a ready-made installer for
# whatever Python version is actually installed, instead of trying (and
# often failing) to compile an old pinned version from source.
pypdf==5.1.0
openpyxl==3.1.5
python-docx==1.1.2
psutil>=6.1.0
rapidfuzz>=3.9.0,<4.0.0
PyYAML>=6.0.2
python-dotenv==1.0.1
playwright==1.62.0
anthropic>=0.40.0
